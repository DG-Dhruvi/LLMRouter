# Build a custom router

Outline
1. Create `custom_routers/<name>/`
2. Implement a `Router` class with `route_single` and `route_batch`
3. Add an optional `Trainer` class
4. Create a config file
5. Verify with `llmrouter list-routers` and `llmrouter infer`
